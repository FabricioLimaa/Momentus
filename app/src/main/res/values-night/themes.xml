<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Momentus" parent="Theme.Material3.Dark.NoActionBar">
        <!-- As customizações de cores primárias, etc., agora são feitas em Compose -->
    </style>

    <!-- Tema para Activities que devem parecer um diálogo -->
    <style name="Theme.Momentus.Dialog" parent="Theme.Momentus">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>
    
</resources>
