<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <application
        android:name=".MomentusApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Momentus"
        tools:targetApi="31">

        <!-- Remove o inicializador padrão do WorkManager para usar a inicialização sob demanda -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                tools:node="remove" />
        </provider>

        <!-- ÚNICA ACTIVITY DE PONTO DE ENTRADA -->
        <activity
            android:name=".ui.screens.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Momentus">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ACTIVITIES QUE AINDA PRECISAM SER MIGRADAS -->
        <activity
            android:name=".ui.screens.ReminderActivity"
            android:exported="false"
            android:theme="@style/Theme.Momentus.Dialog" />
        <activity android:name=".ui.screens.CreateTemplateActivity" android:exported="false" />
        <activity android:name=".ui.screens.EditorRotinaComposeActivity" android:exported="false" />
        <activity android:name=".ui.screens.TemplateDetailActivity" android:exported="false" />

        <service
            android:name=".services.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <activity
            android:name=".ui.screens.WidgetConfigurationActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".notifications.AlarmReceiver"
            android:enabled="true"
            android:exported="false"
            android:directBootAware="true"
            tools:ignore="UnusedAttribute" />

        <receiver
            android:name=".notifications.NotificationActionReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="br.com.fabriciolima.momentus.ACTION_MARK_AS_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Glance Widget -->
        <receiver
            android:name=".widget.MomentusGlanceWidgetReceiver"
            android:exported="true"
            android:configure="br.com.fabriciolima.momentus.ui.screens.WidgetConfigurationActivity">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/momentus_widget_info" />
        </receiver>

    </application>

</manifest>